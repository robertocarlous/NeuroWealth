apiVersion: v1
kind: Service
metadata:
  name: neurowealth-backend
  namespace: neurowealth
  labels:
    app.kubernetes.io/name: neurowealth-backend
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: neurowealth-backend
  ports:
    - name: http
      port: 3001
      targetPort: http
      protocol: TCP
