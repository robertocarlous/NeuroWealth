/**
 * Skip link href, dashboard shell <main id>, and focus target after “Skip to main”
 * share one id so the hash target cannot drift from the landmark.
 */
export const MAIN_CONTENT_LANDMARK_ID = "main-content";

/** TopHeader route title; referenced by dashboard <main aria-labelledby>. */
export const DASHBOARD_ROUTE_TITLE_ID = "dashboard-route-title";
